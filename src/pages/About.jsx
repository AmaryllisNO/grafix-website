import Header from '../components/Header';

const About = () => {
  document.title = 'Grafix | About';

  return (
    <div>
      <Header></Header>
    </div>
  );
};

export default About;
