import Header from '../components/Header';

const Admin = () => {
  document.title = 'Grafix | Admin';
  return (
    <div>
      <Header></Header>
    </div>
  );
};

export default Admin;
